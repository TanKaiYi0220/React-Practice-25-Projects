# Learning Note

## React
- custom hook function are started with `use{}`
- localStorage is the browser variable

## HTML
- `data-theme={var}` can be setup theme variable quickly with `[data-them="{var}"] in css

## CSS
- `data-theme={var}` can be setup theme variable quickly with `[data-them="{var}"] in css
- glow effect: `.contextContainer::before` can be implemented easily by `box-shadow: 0 0 24px var(--accent)`