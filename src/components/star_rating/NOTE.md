# Learning Note

## React
- `[...Array(numberStars)].map(() => {})` to do enumerate implementation in a quick way

## HTML
- `onMouseEnter` and `onMouseLeave` practice

## CSS
- `transform: translateY(-3px) scale(1.12);` which improve UX while interactive star rating