# Learning Note

## React
- [x] use `<form> with <input> and <button>` instead of `<div> with <input> and <button>`
    - [x] with `<form>` can be also trigger by Enter, not only with `<button onClick={} />` 

## CSS