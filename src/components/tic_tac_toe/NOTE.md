# Learning Note

## React
- [x] use `useReducer()` to handle multiple state variable
- [x] `gameLogic` this kind of function w/o UI element declaration can be defined at `.js` file instead of `.jsx`

## CSS
- [x] use `position: relative` and `positon: absolute` to place dropdown box at right position 