# Learning Note

## React
- [x] use `useContext()` for passing props to multiple children
    - [x] In practical cases: it might fit to `<Navbar /> <Sidebar /> <Dashboard /> <Settings /> etc.` in one page.